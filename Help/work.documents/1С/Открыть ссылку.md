Создать реквизит "СсылкаНаДокумент" и добавить команду "ОткрытьСсылку"

![[Pasted image 20260917094131.png]]

Создать процедуру для команды и описать код

```1c
&НаКлиенте
Процедура ОткрытьСсылку(Команда)
	Если НЕ ПустаяСтрока(Строка(Объект.СсылкаНаДокумент))Тогда
		Ссылка = Строка(Объект.СсылкаНаДокумент);
		ПерейтиПоНавигационнойСсылке(Ссылка);
	КонецЕсли;
КонецПроцедуры
```

